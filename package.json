{
  "name": "zoom-question-tracker-root",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && node server.js\" \"cd frontend && npm run dev\"",
    "render-build": "cd frontend && npm ci && npm run build && cd ../backend && npm ci",
    "start:prod": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
